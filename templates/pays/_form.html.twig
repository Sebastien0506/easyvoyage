{{ form_start(form) }}
    {{ form_widget(form) }}
    {# Vérifier si la route est "pays_edit" on affiche les images #}
    {% if app.request.attributes.get('_route') == 'app_pays_edit' %}
    <h2>Images</h2>
    {% for image in pay.paysImages %}
         <div>
             <img src="{{ asset('/uploads/' ~ image.name) }}" alt="Image">
             <a href="{{ path('pays_delete_images', {'id': image.id}) }}" 
             data-delete data-token="{{ csrf_token('delete' ~ image.id )}}">Supprimer</a>
         </div>
    
    {% endfor %}
    
    {% endif %}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
